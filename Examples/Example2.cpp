#include <iostream>

using namespace std;
string characterName = "Daniel"; // this can be changed at a later point later on in the code so that anything following it would change.
string occupation = "who knows.";

int main() {
    cout << "There once was a boy named \n"  << characterName << " who worked as well " << occupation << endl;

    return 0;
}